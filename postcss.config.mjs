/* أُعيد بناء هذا الملفّ لأنّه وصل فارغاً في الأرشيف المرفوع (كان مقطوعاً).
   محتواه قياسي لـ Tailwind 3 + PostCSS 8 ولا يحمل أيّ إعداد خاصّ بالمشروع،
   فإن كان لديك فيه إضافة خاصّة فأعِدها من نسختك. */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
