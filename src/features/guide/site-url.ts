/* ════════════════════════════════════════════════════════════
   عنوان الموقع — إعادة تصدير من المصدر الوحيد

   كان هذا الملفّ يحمل نسخته الخاصّة من العنوان مع الدومين القديم
   مكتوباً بيده. صار العنوان في `@/lib/site-url` وحده، ويبقى هذا الملفّ
   واجهةً له حتى لا يُمسّ من يستورد منه (`/specialties` والبيانات
   المنظّمة فيها) — نقل الدومين لا يستدعي تعديل مساراتِ استيراد قائمة.

   وسببُ وجوده الأصليّ باقٍ ويجب ألّا يُنقض:

   🐛 كانت `absUrl` داخل `guide-store.ts` وأعلاه `"use client"`، فصارت
   **دالّة عميل**. وصفحة `/specialties` مكوّن خادم، فانهار البناء:

     Attempted to call absUrl() from the server but absUrl is on the client.

   الحلّ ليس نقل الصفحة إلى العميل — ذلك يُلغي التصيير على الخادم
   ويُضعف الفهرسة. بل إخراج الدالّة إلى ملفّ **بلا توجيه**، فيستوردها
   الطرفان. و`@/lib/site-url` كذلك بلا توجيه، فالشرط محفوظ.

   قاعدة عامّة: أي دالّة نقيّة يحتاجها الخادم والعميل معاً لا توضع في
   ملفّ يحمل `"use client"`.
════════════════════════════════════════════════════════════ */

export { SITE_URL, absUrl } from "@/lib/site-url";
